using MiRS.Gateway.RunescapeClient;

namespace MiRs.RunescapeClient
{
    class WOMClient : IRuneClient
    {

        public async Task<string> GetRuneUser(string username)
        {
            return string.Empty;
        }
    }
}
